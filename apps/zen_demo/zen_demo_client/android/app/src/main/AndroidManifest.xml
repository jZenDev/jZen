<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:label="zen_demo_client"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- The custom scheme Supabase confirmation and recovery links come back on. A scheme
                 rather than an App Link: App Links need a domain serving assetlinks.json and a
                 signing fingerprint, neither of which a local run has. BROWSABLE is what lets a
                 link opened from a mail client reach the app; the value must match the build's
                 ZEN_AUTH_REDIRECT_URI and the server's AUTH_REDIRECT_URIS exactly.
                 android:launchMode is singleTop above, so a link arriving while the app is already
                 running is delivered to the live activity rather than starting a second one -
                 which is the warm case the app must handle separately from a cold start. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="zendemo" android:host="auth-callback"/>
            </intent-filter>

            <!-- The App Link, which is the same flow with the hijacking removed. A private-use
                 scheme cannot be owned - any app may register "zendemo" and receive a link
                 carrying a live refresh token (RFC 8252 8.6). An https link with autoVerify is
                 different in kind: Android fetches /.well-known/assetlinks.json from the host and
                 opens the app only if it finds this package AND a signing fingerprint that matches
                 the installed build. A hostile app cannot forge that without serving files from
                 the domain.

                 android:host is a build placeholder rather than a literal, because the host IS the
                 deployed environment and no environment fact belongs in the repository (STANDARDS
                 "Deployment model"). It comes from ZEN_APPLINKS_HOST via build.gradle.kts.

                 Both filters coexist on purpose: the scheme keeps local runs working with no
                 domain and no signing, and it is what a developer replays from the terminal. The
                 https filter is what a deployed environment should actually mail. Retiring the
                 scheme in production is a config change (AUTH_REDIRECT_URIS), not a code one. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https"
                      android:host="${zenApplinksHost}"
                      android:pathPrefix="/auth/callback"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
