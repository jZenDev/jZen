# Execution plan: Dart/Flutter code review remediation

**Source:** `docs/plans/DART-FLUTTER-CODE-REVIEW.md` (2026-09-14). 10 ranked findings across
`client/` (`zen_core`, `zen_transport`, `zen_secure_store`, `zen_identity`, `zen_ui_identity`,
`zen_ui_navigation`) and `apps/zen_demo/zen_demo_client` including its platform runners.

This plan turns the review into ordered, independently-landable units of work. Each unit names its
branch, the files it touches, the steps, the tests to add or extend, and whether it trips
`task verify:contracts`, needs an ADR, or needs a `DECISIONS.md` entry. Nothing here is executed
yet — this is the plan to work from, one branch at a time, with the relevant `task test:*` targets
green before each PR.

## Ground rules for every unit below

- One finding (or tightly-coupled pair) per branch, per `CLAUDE.md`: `fix/`, `security/`, or
  `docs/` prefix, never onto `main`.
- `flutter analyze` must stay clean for every touched package, and `task test:client` (plus
  `task test:apps:client` for anything reaching `zen_demo_client`) must pass before a PR is opened.
- `task verify:boundaries` must stay green — nothing here should ever add a direct Supabase call or
  a runtime-config read to the client; none of the ten findings asks for either.
- A hand-edit to a `project.pbxproj` (Finding 1) is not "generated output" in the
  `verify:contracts` sense, but it is semi-generated by Xcode — verify with a real build
  immediately after editing, never just by inspecting the diff.
- Any change that reverses or supersedes a prior architectural decision gets a `DECISIONS.md` entry
  via the `add-adr` skill; none of the ten findings currently looks ADR-sized, but Finding 2's fix
  should note this in its PR description in case dependency-audit policy is later judged decision
  -worthy.

---

## Phase 0 — Unblock the gate (do first; the pattern is documented as having recurred once already)

### Finding 1 — iOS and macOS runners do not build from a clean checkout ✅ DONE

- **Status:** Done — `Pods_Runner*`/`Pods_RunnerTests*` dangling references removed from both
  `project.pbxproj` files, verified with a real build.
- **Branch:** `fix/xcode-runner-stale-pods-references`
- **Files:** `apps/zen_demo/zen_demo_client/ios/Runner.xcodeproj/project.pbxproj`,
  `apps/zen_demo/zen_demo_client/macos/Runner.xcodeproj/project.pbxproj`.
- **Steps:**
  1. Open both projects in Xcode; select `Pods_Runner.framework` and `Pods_RunnerTests.framework`
     under each target's `Frameworks` group and use "Remove Reference" (not "Move to Trash" — there
     is nothing on disk to delete, only the dangling `PBXFileReference`/`PBXBuildFile` entries and
     their `Frameworks` build-phase membership).
  2. Diff `project.pbxproj` afterward and confirm the only removed lines are the
     `Pods_Runner*`/`Pods_RunnerTests*` entries — no unrelated reordering from Xcode's own project
     -file rewrite.
  3. Verify with the exact commands the review used to reproduce the failure:
     `flutter build macos --release --analyze-size` and
     `flutter build ios --debug --no-codesign --simulator`. Both must now succeed.
  4. Also run `task build:apps:runners` (the task that silently broke last time, per its own
     documented history) to confirm the fix covers the actual CI-adjacent entry point, not just the
     two ad hoc `flutter build` invocations.
- **Tests:** no unit test applies; the build success itself is the test. Consider whether
  `Taskfile.app.yml`'s `build:apps:runners` should be added to a CI job that runs on macOS runners,
  since this is the second time this exact failure mode has appeared with nothing to catch it
  automatically — flag this as a follow-up decision (see "Decisions to surface" below) rather than
  scope-creeping it into this branch.
- **verify:contracts:** no.
- **ADR:** no — restores a previously-working state, doesn't change design.
- **Effort:** S (the edit itself) but budget time for a real Xcode/simulator build cycle to verify.
- **Depends on:** nothing. **Should land before:** Finding 4, since both touch the same iOS/macOS
  build surface and a green baseline makes Finding 4's `flutter build web`/font-warning
  verification unambiguous (no unrelated linker failure noise in the same review pass).

---

## Phase 1 — High-severity, contained fixes (independent, can be parallelized across sessions)

### Finding 2 — `task audit` silently does not cover Dart/pub dependencies ✅ DONE

- **Status:** Done — `task audit:client` added (`scripts/audit-client.py`, queries OSV's `Pub`
  ecosystem against both workspace `pubspec.lock` files), wired into the aggregate `audit:` task,
  with fixture tests under `task test:scripts`. Landed on `fix/xcode-runner-stale-pods-references`
  rather than a separate branch, at the user's direction.
- **Branch:** `fix/add-audit-client-task`
- **Files:** `Taskfile.yml` (new `audit:client` task, wired into the aggregate `audit:` task
  alongside `audit:server`/`audit:admin`); possibly a small script under `.claude/tools/` or inline
  `task` shell if pub's own tooling is enough.
- **Steps:**
  1. Establish what pub-ecosystem audit tooling actually exists and is installable without a
     network-dependent one-off (`dart pub outdated --mode=null-safety` doesn't check advisories;
     confirm whether `osv-scanner` — already a plausible fit since the review used `api.osv.dev`
     directly — can run against `pubspec.lock` for every workspace member, or whether a documented
     query against `api.osv.dev` per-package is the honest, lower-tooling option).
  2. Add `audit:client` to `Taskfile.yml`, iterating every Dart/Flutter package the way
     `test:client` already does, and wire it into the aggregate `audit:` task.
  3. Confirm it is genuinely green today against the five packages the review already checked by
     hand (`flutter_secure_storage`, `app_links`, `protobuf`, `http`, `web_socket_channel`) plus the
     rest of the dependency graph the review didn't have time to check package-by-package.
  4. Update the `task --list` table in `CLAUDE.md` if `audit:client` becomes independently
     invokable (it already documents `task audit`, `task deps`, etc.).
- **Tests:** none in the unit-test sense; the task's own green/red result is the check. Add a
  negative-path sanity check (temporarily pin a package to a version with a known OSV advisory,
  confirm the task fails, then revert) during development — do not leave that pin in the committed
  diff.
- **verify:contracts:** no. **ADR:** no.
- **Effort:** M — the work is mostly discovering what tooling is actually usable here, not the
  `Taskfile.yml` wiring itself.
- **Depends on:** nothing.

### Finding 3 — Android release build is signed with the debug keystore ✅ DONE

- **Status:** Done — decided (user call) that `zen_demo` does not need a real release keystore: it
  is a reference/demo app that never ships to the Play Store. Replaced the `TODO` in
  `build.gradle.kts` with a comment documenting the debug-signing as an intentional demo-scope
  limitation, with the follow-up path spelled out if real distribution is ever needed. No
  `key.properties`/keystore generated. Landed on `fix/xcode-runner-stale-pods-references` rather
  than a separate branch, at the user's direction.
- **Branch:** `fix/android-release-signing-config`
- **Files:** `apps/zen_demo/zen_demo_client/android/app/build.gradle.kts`; new
  `apps/zen_demo/zen_demo_client/android/key.properties` (gitignored, never committed — the
  keystore file itself lives outside the repo, e.g. in a secrets manager or the deploy runner's
  local disk); `apps/zen_demo/zen_demo_client/android/.gitignore` if `key.properties` isn't already
  covered by a broader pattern.
- **Steps:**
  1. Generate a real upload/release keystore (`keytool -genkey -v -keystore ... -alias zen_demo`) —
     this is a one-time, human-run step outside this session (a keystore is a secret; treat it the
     same way the deploy skill treats `SUPABASE_ACCESS_TOKEN` — required, never generated or stored
     by the agent).
  2. Add `key.properties` (gitignored) holding the keystore path/passwords, following the standard
     Flutter pattern.
  3. In `build.gradle.kts`, read `key.properties`, register a real `signingConfigs.create("release")`,
     and point `buildTypes.release.signingConfig` at it instead of `signingConfigs.getByName("debug")`.
     Remove the `// TODO: Add your own signing config` comment now that it's resolved.
  4. Re-run `flutter build appbundle --release --analyze-size --target-platform=android-arm64` and
     confirm the build still succeeds and is no longer debug-signed (`apksigner verify --print-certs`
     or equivalent on the output, or Gradle's own signing report).
- **Tests:** no unit test; the build's own signing verification is the check.
- **verify:contracts:** no. **ADR:** no.
- **Effort:** S for the Gradle change; the keystore generation/custody question is the real
  decision — see "Decisions to surface" below (who holds the keystore, and whether `zen_demo` as a
  reference app should even ship a real one versus documenting the gap as intentionally open for a
  demo that never reaches the Play Store).
- **Depends on:** nothing technically, but should not be started until the keystore-custody
  question is answered (no point wiring a `key.properties` path with nothing behind it).

### Finding 4 — `CupertinoIcons` glyphs used by shared navigation code are not bundled ✅ DONE

- **Status:** Done — `cupertino_icons: ^1.0.9` added to `zen_ui_navigation/pubspec.yaml` (matching
  the version already pinned transitively). `task deps` re-resolved both workspaces;
  `flutter build web --release --wasm --tree-shake-icons` now tree-shakes `CupertinoIcons.ttf`
  cleanly (257628 → 1472 bytes) with no "Expected to find fonts" warning. `flutter analyze` clean
  for `zen_ui_navigation`. Landed on `fix/xcode-runner-stale-pods-references` rather than a
  separate branch, at the user's direction.
- **Branch:** `fix/declare-cupertino-icons-dependency`
- **Files:** `client/zen_ui_navigation/pubspec.yaml` (add `cupertino_icons: ^1.x` — check the
  version already pinned transitively in `client/pubspec.lock` and match it unless a newer patch is
  warranted).
- **Steps:**
  1. Add the dependency to `zen_ui_navigation/pubspec.yaml`, the package that actually imports
     `flutter/cupertino.dart` and references `CupertinoIcons.ellipsis` / `CupertinoIcons.checkmark_alt`.
  2. Run `task deps` to resolve the new dependency across both pub workspaces
     (`client/` and `apps/`), confirming `zen_demo_client`'s lockfile picks it up transitively.
  3. Re-run `flutter build web --release --wasm --tree-shake-icons` and confirm the
     `Expected to find fonts for (MaterialIcons, packages/cupertino_icons/CupertinoIcons)` warning
     from the review is gone.
  4. Spot-check on a real iOS/macOS build (now unblocked by Finding 1) that the "more" tab icon and
     the overflow-page checkmark render as real glyphs, not tofu.
- **Tests:** `flutter analyze` and existing `zen_ui_navigation` widget tests should be unaffected;
  no new test is needed for a font-asset fix, but note in the PR description that the fix was
  confirmed by the build warning disappearing (this review's own standard for "real evidence, not
  an assumption").
- **verify:contracts:** no. **ADR:** no.
- **Effort:** XS.
- **Depends on:** Finding 1 landed first is preferable (a clean iOS/macOS build makes the visual
  spot-check possible without an unrelated linker failure in the way), but not a hard blocker —
  the web build alone is enough to confirm the font warning is resolved.

---

## Phase 2 — Medium/low, contained fixes (independent of each other and of Phase 0/1 except where noted)

### Finding 5 — `IdentityContract.toJson`/`.fromJson` are dead code and lose `emailVerified` ✅ DONE

- **Branch:** `fix/remove-dead-identity-contract-json`
- **Files:** `client/zen_identity/lib/src/identity_contracts.dart` (delete `toJson`/`fromJson` on
  `IdentityContract`).
- **Steps:**
  1. Confirm (re-run the review's own check) that
     `grep -rn "IdentityContract.fromJson\|\.toJson()" client/ apps/` still shows no caller outside
     the file itself.
  2. Delete both methods and any now-unused imports (`dart:convert` if nothing else in the file
     needs it).
  3. Do **not** add a replacement JSON round-trip — the wire format is proto, per this codebase's
     transport design; the fix is deletion, not repair, unless a real caller is found in step 1 that
     the review missed.
- **Tests:** `task test:client` for `zen_identity` should be unaffected (no test currently exercises
  these methods, consistent with "dead code"). If a test does reference them, that contradicts the
  review's finding — stop and re-check before deleting.
- **verify:contracts:** no. **ADR:** no. **Effort:** XS.
- **Depends on:** nothing.

### Finding 6 — Native deep-link handler forwards any URI with no scheme/host check ✅ DONE

- **Status:** Done — `isRegisteredAuthLink` guard added in `auth_deep_links_native.dart`. It
  accepts the custom scheme from `zenAuthRedirectUri` (host `auth-callback`) and, since
  `AndroidManifest.xml` also registers a real `https` App Link (`pathPrefix="/auth/callback"`,
  a build-time host per STANDARDS "Deployment model") that the plan's single-scheme check would
  have silently broken in deployed environments, the `https` App Link path too. Rejections are
  logged via `ZenLogger.instance.debug`. New test:
  `apps/zen_demo/zen_demo_client/test/auth_deep_links_native_test.dart`.
- **Branch:** `fix/deep-link-scheme-host-filter`
- **Files:** `apps/zen_demo/zen_demo_client/lib/src/auth_deep_links_native.dart`.
- **Steps:**
  1. In the handler that receives `AppLinks().getInitialLink()` / `.uriLinkStream` events, add a
     guard before calling `onLink`/`consumeAuthLink`: reject anything whose `scheme != 'zendemo'` or
     `host != 'auth-callback'` (match the actual host segment the app's own registered links use —
     confirm the exact expected host from `AndroidManifest.xml`'s intent filter and the iOS/macOS
     `Info.plist` URL scheme entries before hard-coding it).
  2. Do **not** touch `ZenAuthLink.parse` in `zen_identity` — the review is explicit that the
     token-bearing path is already correctly re-validated server-side (ADR-018/ADR-019); this fix is
     scoped to the app-level forwarding filter only, not the framework parser.
  3. Log (at debug level) any rejected URI so a legitimate new deep-link path added later isn't
     silently swallowed without a trace during development.
- **Tests:** new test (or extend an existing `auth_deep_links_native` test if one exists — check
  first, since the review doesn't mention one) asserting a URI with the wrong scheme or wrong host
  is dropped before reaching `consumeAuthLink`, and the legitimate
  `zendemo://auth-callback?auth=email-confirmed` case still passes through.
- **verify:contracts:** no. **ADR:** no. **Effort:** S.
- **Depends on:** nothing. Low severity — fine to schedule opportunistically.

### Finding 7 — `zen_logger_test.dart` asserts nothing

- **Branch:** `fix/zen-logger-test-real-assertions`
- **Files:** `client/zen_core/test/zen_logger_test.dart`; possibly a small test-only fake
  `ZenLoggerStrategy` if one doesn't already exist for injection.
- **Steps:**
  1. Check whether `ZenLogger` already supports injecting a strategy for testing (a constructor
     param, a settable singleton strategy, etc.) — read `zen_logger.dart` before writing a fake.
  2. Add a fake `ZenLoggerStrategy` that captures the formatted message, level, and origin passed to
     it.
  3. Rewrite both existing test bodies to call `ZenLogger.instance.debug(...)` / whichever methods
     are covered, then `expect()` on the captured output: the `[DEBUG]`/`[INFO]` prefix, the
     `internalData` interpolation, and the `origin` handling the review specifically names as
     currently unverified.
  4. If real behavioral assertions turn out to be impractical for some call (e.g. a method that only
     has a side effect on an unobservable global), fall back to the review's stated alternative:
     rename the test to say it's a smoke test and accept the narrower scope explicitly, rather than
     leaving it reading like coverage it doesn't provide.
- **Tests:** this branch *is* the test change; no separate test needed.
- **verify:contracts:** no. **ADR:** no. **Effort:** S.
- **Depends on:** nothing.

### Finding 8 — Duplicated, weaker client-side email validation

- **Branch:** `fix/shared-email-validator-in-ui`
- **Files:** `client/zen_ui_identity/lib/src/screens/login_screen.dart`,
  `register_screen.dart`, `restore_password_screen.dart`.
- **Steps:**
  1. Check `client/zen_core/lib/src/value_objects/common_types.dart`'s `EmailAddress.create` return
     shape (likely a `Result`/`ZenResult`-style type) and decide the cleanest call site: either
     `EmailAddress.create(value).isFailure` directly in each `validator:` callback, or a thin
     `bool isValidEmail(String value)` helper in `zen_core` if the `Result` unwrapping reads awkwardly
     three times.
  2. Replace `if (!value.contains('@')) return messages.validationEmail;` in all three files with the
     shared check, keeping the same localized `messages.validationEmail` string on failure.
  3. Confirm no other UI-layer email check exists elsewhere in `zen_ui_identity` that the review
     didn't list (grep for `.contains('@')` across the package to be sure only these three sites
     exist).
- **Tests:** extend each screen's existing widget test (if present) with a case like `a@` (passes
  the old check, must now fail) to lock in the strictness change; if no widget test currently
  exercises the validator callback directly, add a small one per screen.
- **verify:contracts:** no. **ADR:** no. **Effort:** S.
- **Depends on:** nothing. Can land alongside Finding 5/7 in the same review pass since all three
  are small and touch disjoint files.

### Finding 10 — no task exercises `--release --obfuscate --split-debug-info` for any client runner

- **Branch:** `docs/manual-obfuscated-release-check` (or `fix/...` if a Taskfile target is added —
  see decision below).
- **Files:** either a new section in a relevant skill/doc (e.g. `.claude/skills/deploy` or
  `Taskfile.app.yml`'s own comments) documenting the manual check, or — if the decision below favors
  automation — a new `build:runners:release` task in `Taskfile.app.yml` alongside the existing
  debug-only `build:runners`.
- **Steps (doc-only path):**
  1. Run `flutter build <target> --release --obfuscate --split-debug-info=<dir>` by hand for at
     least one native target reachable from this machine (macOS, once Finding 1 is fixed — Android
     is the other candidate and doesn't depend on Finding 1).
  2. Record the result (pass/fail, and the command) in a doc note near `build:runners`'s own
     "this is a COMPILE check, artifacts are throwaway" comment, so the gap is named rather than
     silently assumed closed.
- **Steps (task path, if chosen instead):**
  1. Add `build:runners:release` mirroring `build:runners` but with `--release --obfuscate
     --split-debug-info=<throwaway dir>` per target, understanding this will cost meaningfully more
     build time (a full release compile, not a debug one) — confirm with the `long-job` skill's
     measured-duration convention before wiring it into a path that runs often.
  2. Decide whether it belongs in `task build` (every run) or as a separate, opt-in task (periodic
     manual/CI check) — the review's own fix suggestion leans toward "a follow-up task or a periodic
     manual check," not necessarily every-build automation.
- **Tests:** none; the build's own success is the check either way.
- **verify:contracts:** no. **ADR:** no.
- **Effort:** XS (doc-only) or M (new Taskfile target, given the build-time cost to measure and
  document).
- **Depends on:** Finding 1, if the chosen target is iOS/macOS; independent if Android is used
  instead (Android already builds cleanly per the review's evidence).

---

## Not scheduled (informational or already resolved by design)

- **Finding 9 — Web bundle size (48 MB, CanvasKit ~41 MB).** No action: the review confirms this
  matches ADR-016's documented, deliberate figure. Nothing to schedule.
- **`profile_screen.dart:71` `subject.substring(0, 1)` unguarded-empty-string nit** (noted in the
  review's app-code section, not in the top 10). Not currently reachable per the review's own check
  of `SupabaseIdentityRepository._toContract`. Worth a one-line defensive guard
  (`identity.id.value.isEmpty ? '?' : identity.id.value.substring(0, 1)`) if anyone is already
  touching `profile_screen.dart` for another reason, but not worth its own branch.

---

## Suggested sequencing summary

| Order | Item | Why here |
|---|---|---|
| 1 | Finding 1 ✅ DONE | Blocks two whole platforms from a clean checkout; documented as a recurring failure mode |
| 2 | Finding 4 | Small, and easiest to verify visually once Finding 1 unblocks a real iOS/macOS build |
| 3 | Finding 5, Finding 7, Finding 8 | Small, independent, disjoint files — good batch for one review pass |
| 4 | Finding 2 ✅ DONE | Closes a real audit blind spot; sequenced after the quick wins so it gets full attention for the tooling-discovery work |
| 5 | Finding 3 ✅ DONE | Needs the keystore-custody decision first (see below) — start the Gradle wiring only after that's answered |
| 6 | Finding 6 ✅ DONE | Low severity, no dependencies, fine to slot in whenever |
| 7 | Finding 10 | Needs the doc-vs-task decision first; naturally follows Finding 1/3 since it benefits from both native targets already building |

## Decisions to surface to the user before scheduling

1. **Finding 1** — after the `pbxproj` fix, should `task build:apps:runners` (or an equivalent
   `flutter build macos`/`flutter build ios --simulator` check) run in CI on a macOS runner so this
   exact regression can't recur silently a third time? The review calls this out as having already
   happened once.
2. **Finding 3** — who generates and holds the `zen_demo` Android release keystore, and does a
   reference/demo app that isn't shipping to the Play Store need a real one at all, versus
   documenting the debug-signing as an intentional demo-scope limitation? Both are defensible; pick
   one before wiring `key.properties`.
3. **Finding 2** — is a real advisory-scanning tool (e.g. `osv-scanner` against `pubspec.lock`)
   installable in this environment/CI, or should `audit:client` be a documented, scripted query
   against `api.osv.dev` per package (lower tooling burden, more code to maintain)?
4. **Finding 10** — should the obfuscated release build become an automated (if infrequent) Taskfile
   target, or stay a documented manual check performed before the first real distribution build?
   The review's own fix suggestion leans toward the latter, but automation is worth considering now
   that Finding 1 will make the native targets buildable again.
