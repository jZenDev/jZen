<?xml version="1.0" encoding="UTF-8"?>
<!--
  zen-core :: the framework-free Java core.

  Holds the ZenResult / ZenError result types, ZenStatus, the AcceptLanguage parser, and
  ZenLocales - the pieces every other module may depend on.

  Deliberately has no JAX-RS, no Panache, and no transport dependency. That isolation is the
  single most valuable property of the module: it is what lets any module use these types
  without inheriting a framework. Do not add framework deps here.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>zen</groupId>
    <artifactId>zen-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>zen-core</artifactId>
  <name>jZen :: core</name>

  <dependencies>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-junit5</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <!--
        Flags a fully-qualified class reference used inline where an import would do (issue #61).
        The plugin version is pinned once in zen-parent's pluginManagement; each consuming module
        opts in here with its own execution, so resolving a new zen-parent never silently starts
        linting a module that hasn't opted in. Bound to test-compile (not compile/verify): a
        fully-qualified reference compiles fine, so compile never catches it, and test-compile is
        the last phase a plain `mvn test` and a full `mvn package`/native build both pass through.
      -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-pmd-plugin</artifactId>
        <configuration>
          <rulesets>
            <ruleset>${project.basedir}/../pmd-ruleset.xml</ruleset>
          </rulesets>
          <includeTests>true</includeTests>
          <excludeRoots>
            <excludeRoot>${project.build.directory}/generated-sources/annotations</excludeRoot>
          </excludeRoots>
        </configuration>
        <executions>
          <execution>
            <phase>test-compile</phase>
            <goals>
              <goal>check</goal>
            </goals>
            <configuration>
              <failOnViolation>true</failOnViolation>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
